allowBuilds:
  unrs-resolver: set this to true or false
onlyBuiltDependencies:
  - unrs-resolver

# Allow build scripts for approved dependencies without interactive prompt
# Required for CI with --frozen-lockfile
